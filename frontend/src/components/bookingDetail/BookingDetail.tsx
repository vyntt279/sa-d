type BookingDetailProps = {
  id: number,
  userName: string,
  roomName: string,
  date: string
}

const BookingDetail = (props: BookingDetailProps) => {
  const { id, userName, roomName, date } = props
  return (
    <></>
  )
}

export default BookingDetail;